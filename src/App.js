import WeatherCard from "./components/weatherCard";

function App() {
  return (
    <div className="bg-gradient-to-l md:bg-gradient-to-r">
      <WeatherCard />
    </div>
  );
}

export default App;
